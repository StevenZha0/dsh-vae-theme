# 版权说明 —— 公开分发前必读

本主题**代码**以 MIT 发布，但主题内嵌的**图像与文本**来源不同，发布前请逐项确认。

## 一、可随 MIT 一同分发（本项目程序生成）

以下资产全部由 `make-*.mjs` 用 Node + sharp + SVG 滤镜**程序生成**，不含第三方素材，
可按 MIT 授权：

| 资产 | 生成脚本 |
|---|---|
| 主界面水墨长卷 | `make-scroll.mjs` |
| 金丝九宫格边框（角 + 边条） | `make-frames.mjs` |
| Cordis 面板专属水墨夜景（月牙 + 题字） | `make-frames.mjs` |
| VAE 书法标志、金色导航字 | `make-logo.mjs` |
| 输入区 / 新会话按钮金丝玻璃 | `make-panels-v2.mjs` |
| 浮窗面板裁切 | `make-panels-v2.mjs`（长卷裁切） |

> 注：字体使用了系统自带的华文行楷 / 华文楷体。**若把字形烘焙进图片分发**，
> 严格说涉及字体授权；如需完全干净，可改用开源字体（如「霞鹜文楷」「思源宋体」）重出。

## 二、第三方素材（当前分发状态）

本节记录主题里**哪些内容来自第三方**，以及各自的当前状态。
这些内容**均随包分发**（内嵌于 `lib/theme.tczp`），版权归各自权利人所有。

| 资产 | 位置 | 状态 |
|---|---|---|
| 人物肖像照片 | `areas.sidebar`（520×1800） | **随包分发**：原图为摄影作品，版权属摄影师/出版方，肖像权属本人 |
| 专辑封面墙 | `areas.details`（1400×1400） | **随包分发**：由 28 张专辑封面拼成（网易云音乐 CDN），版权属唱片公司 |
| 歌词文本 | `lyrics.mjs`（内联进 `runtime.js` 与 `.tczp`） | **随包分发**：许嵩本人作词的中国风作品，版权归许嵩先生所有 |

> 以上三项**都是随包分发的**。本节此前的写法把肖像与专辑墙记为"已移除"，
> 那是错误的——经解码 `lib/theme.tczp` 内嵌图核实，两者均在包中。
> 此处按实际形态更正。作者对这些内容的收录与呈现所作的说明，见下一节。

### 关于第三方内容：本节的历史说明

本节此前写着上述素材「不可随包分发，公开发布前必须替换或移除」。**该说明已不适用于当前发布形态**，
原因与措辞如下，均由本项目作者作出：

主题以**粉丝向非商业作品**的形式发布，肖像、专辑封面与歌词均取自公开来源，
在主题内分别作为侧边栏背景、设置面板拼贴与竖排歌词轮播呈现，歌词逐条标注出处（曲名）。
本项目作者认为这一形态下的收录与呈现是适当且可辩护的，
因此这些内容**随 npm 包与主题预设一同分发**。

这是在说明本项目作者的编辑选择，**不是一份法律结论**——
作者不对「该使用是否构成合理使用或已获授权」作出任何陈述，
第三方素材的版权始终归各自权利人所有。相关免责声明见 README「版权说明」一节与本文第三节。

### 想自行取用或替换

- 歌词：全部集中在 `lyrics.mjs`，按曲目分组，替换 `SONGS` 数组后重新构建即可；
- 肖像 / 专辑墙：由 `make-sidebar.mjs`、`make-assets*.mjs` 从 `raw/` 下的原始素材生成，
  替换 `raw/` 中的图片后重新生成即可；也可把对应区域的 `mode` 设为 `none` 去掉该背景；
- 关掉全部页面特效：`localStorage.setItem('vae-theme-fx','off')` 后刷新。

### 快速清理清单

```bash
# 去掉肖像（侧边栏改为纯墨底）
#   编辑 make-theme.mjs，把 areas.sidebar.mode 设为 'none'
# 去掉专辑墙（设置面板改为纯色）
#   编辑 make-theme.mjs，把 areas.details.mode 设为 'none'
# 去掉歌词
#   编辑 lyrics.mjs 替换文本，或把 LYRICS 置空并在 runtime 里关掉歌词特效
```

## 三、商标与关联声明

本主题为**粉丝向个人作品**，与许嵩本人、其经纪公司及 DeepSeek 均无关联，
不使用其官方标识。README 中建议加一句免责声明：

> 本主题为个人兴趣作品，与许嵩先生及其经纪公司无任何关联。
> 主题内使用的素材版权归各自权利人所有。
