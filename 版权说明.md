# 版权说明 —— 公开分发前必读

本主题**代码**以 MIT 发布，但主题内嵌的**图像与文本**来源不同，发布前请逐项确认。

## 一、可随 MIT 一同分发（本项目程序生成）

以下资产全部由 `make-*.mjs` 用 Node + sharp + SVG 滤镜**程序生成**，不含第三方素材，
可按 MIT 授权：

| 资产 | 生成脚本 |
|---|---|
| 主界面水墨长卷 | `make-scroll.mjs` |
| 金丝九宫格边框（角 + 边条） | `make-frames.mjs` |
| Cordis 面板专属水墨夜景（月牙 + 题字） | `make-frames.mjs` |
| VAE 书法标志、金色导航字 | `make-logo.mjs` |
| 输入区 / 新会话按钮金丝玻璃 | `make-panels-v2.mjs` |
| 浮窗面板裁切 | `make-panels-v2.mjs`（长卷裁切） |

> 注：字体使用了系统自带的华文行楷 / 华文楷体。**若把字形烘焙进图片分发**，
> 严格说涉及字体授权；如需完全干净，可改用开源字体（如「霞鹜文楷」「思源宋体」）重出。

## 二、不可随包分发（第三方版权）

以下素材来自网络，**版权不属于本项目**。公开发布前必须替换或移除：

| 资产 | 位置 | 说明 |
|---|---|---|
| **人物肖像照片** | `areas.sidebar` → `许嵩-侧边栏.webp` | 原图为摄影作品（`raw/web/5be2be1c7a38.jpg`），版权属摄影师/出版方；肖像权属本人 |
| **专辑封面** | `areas.details` → `专辑墙-设置界面.webp` | 由 28 张专辑封面拼成（`raw/albums/*.jpg`，来自网易云音乐 CDN），版权属唱片公司 |
| **歌词文本** | `lyrics.mjs` | 许嵩歌词的著作权属其本人/版权方 |

### 处理建议

**仅在你自己机器上用**：现状即可，无需处理。

**要开源/发布到插件市场**，选择其一：

1. **替换为可分发素材**（推荐）
   - 肖像 → 换成程序生成的水墨人物剪影，或留空（`areas.sidebar.mode = 'none'`）
   - 专辑墙 → 换成程序生成的「山水小景拼贴」，或纯色/纹理
   - 歌词 → 换成**公版古典诗词**（如《全唐诗》《宋词》中早已进入公有领域的作品），
     并在 README 注明出处。这也更契合「庐州月」的国风定位

2. **让使用者自备素材**
   - 包里只带程序生成的背景与边框
   - README 说明「把肖像放到 `raw/web/`、专辑封面放到 `raw/albums/`，再跑 `node make-assets*.mjs`」

3. **只发主题预设、不发素材**
   - 不推荐，因为 `.tczp` 本身就把图片内嵌了

### 快速清理清单

```bash
# 移除肖像（侧边栏改为纯墨底）
#   编辑 make-theme.mjs，把 areas.sidebar.mode 设为 'none'
# 移除专辑墙（设置面板改为纯色）
#   编辑 make-theme.mjs，把 areas.details.mode 设为 'none'
# 移除歌词
#   编辑 lyrics.mjs，替换为公版诗词，或把 LYRICS 置空并在 runtime 里关掉歌词特效
```

## 三、商标与关联声明

本主题为**粉丝向个人作品**，与许嵩本人、其经纪公司及 DeepSeek 均无关联，
不使用其官方标识。README 中建议加一句免责声明：

> 本主题为个人兴趣作品，与许嵩先生及其经纪公司无任何关联。
> 主题内使用的素材版权归各自权利人所有。
